{
  "name": "larvago-nuvio-providers",
  "version": "1.0.0",
  "description": "Providers para Nuvio",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "nodemon --watch src -e js,json --exec 'node build.js'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "esbuild": "^0.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
