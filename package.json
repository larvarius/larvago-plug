{
  "name": "larvago-streamix-providers",
  "version": "1.0.0",
  "description": "Streamix provider for Nuvio",
  "scripts": {
    "build": "node build.js",
    "build:watch": "nodemon --watch src -e js,json --exec 'node build.js'"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "nodemon": "^3.0.0"
  },
  "dependencies": {
    "axios": "^1.14.0"
  },
  "main": "build.js",
  "type": "commonjs"
}
